DATABASE_URL="file:./dev.db"

# Optional: enables server-side model vision date recognition before local OCR fallback.
# Images are sent to OpenAI only when this key is configured.
OPENAI_API_KEY=""
OPENAI_DATE_MODEL="gpt-4.1"
